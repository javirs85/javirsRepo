<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:gameBrain ="clr-namespace:GameBrainControl;assembly=GameBrain"
             x:Class="GB.AutoPuzzlesPage" x:Name="APuzzlesPage"
             Focused="ContentPage_Focused">

    <StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label>Here comes the list with</Label>
            <Label x:Name="ListSize" BindingContext="{x:Static gameBrain:GameBrain}" Text="{Binding PuzzlesCount, StringFormat= '{0}'}"></Label>
            <Label>elements</Label>
        </StackLayout>
        <ListView x:Name="PuzzlesContainer">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Height="100">
                        <Label HeightRequest="80" BackgroundColor="Red" TextColor="Black" Text="{Binding Path=Name}"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>